version: '3.7'

volumes:
 hm-db_vol01:
 hm-web_vol01:
 hm-logs_vol01:
 #ssl-certificates_vol01:

#networks:
#  traefik:
#   name: traefik
#   external: true
#  local_stack:

services:

 hm-db:
    image: docker.adipsys.com:5000/hm-ha-db
    restart: always
    command : seed
    # command: no-galera
    # command: node hm-db
    environment:
      - XTRABACKUP_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_ROOT_HOST=%
      - NODE_ADDRESS=^10.0.*.*
      #- FAKE_HEALTHCHECK=8081
    deploy:
      placement:
             #max_replicas_per_node: 1
             constraints:
                 - node.role == manager
    #networks:
      #       - local_stack
      
    volumes:
          - 'hm-db_vol01:/var/lib/mysql/'

# hm-db-seed:
#    image: docker.adipsys.com:5000/hm-ha-db
#    restart: always
#    command: seed
#    # command: node hm-db
#    environment:
#      - XTRABACKUP_PASSWORD=${MYSQL_ROOT_PASSWORD}
#      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
#      - MYSQL_ROOT_HOST=%
#      - NODE_ADDRESS=^10.0.*.*
#    deploy:
#      placement:
#             #max_replicas_per_node: 1
#             constraints:
#                 - node.role == manager
#    volumes:
#          - 'hm-db-vol01:/var/lib/mysql/'
 
# hm-db:
#    image: docker.adipsys.com:5000/hm-ha-db
#    restart: always
#    command: node hm-db-seed,hm-db
#    environment:
#      - XTRABACKUP_PASSWORD=${MYSQL_ROOT_PASSWORD}
#      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
#      - MYSQL_ROOT_HOST=%
#      - NODE_ADDRESS=^10.0.*.*
#      - HEALTHY_WHILE_BOOTING=1
#
#    deploy:
#       replicas: 0
#    #  placement:
#             #max_replicas_per_node: 1
#             #constraints:
#             #    - node.role == manager
#    volumes:
#          - 'hm-db_vol01:/var/lib/mysql/'
          
 hm-web:
      image: docker.adipsys.com:5000/hm-ha-web:${PRODUCT_VERSION}
      hostname: wifi-node1
      restart: always
      environment:
             - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
             - OPERATOR=${OPERATOR}
             - HM_SERVER_NAME=${HM_SERVER_NAME}
             - HM_URL_SUFFIX=${HM_URL_SUFFIX}
             - HOTSPOT_ALIAS1=${HOTSPOT_ALIAS1}
             - ADMIN_ALIAS1=${ADMIN_ALIAS1}
             - OPERATOR_SUPPORT_EMAIL=${OPERATOR_SUPPORT_EMAIL}
             - LICENCE_SERVER=${LICENCE_SERVER}
             - RSYSLOG_SERVER=${RSYSLOG_SERVER}
             - REMEDIATION=yes
             - SYMFONY_VERSION=1.5.11
             #- LEGACY_MODE=1
      deploy:
         placement:
             #max_replicas_per_node: 1
             constraints:
                 - node.role == manager
      #   labels:
      #       - "traefik.enable=true"
      #       - "traefik.http.routers.${OPERATOR}-http.rule=Host(`${OPERATOR}.${HM_URL_SUFFIX}`)"
      #       - "traefik.http.routers.${OPERATOR}-http.entrypoints=web"
      #       - "traefik.http.routers.${OPERATOR}-https.rule=Host(`${OPERATOR}.${HM_URL_SUFFIX}`)"
      #       - "traefik.http.routers.${OPERATOR}-https.entrypoints=websecure"
      #       - "traefik.http.services.${OPERATOR}-http.loadbalancer.server.port=80"
      #       - "traefik.http.services.${OPERATOR}-http.loadbalancer.server.scheme=http"
      #       - "traefik.http.services.${OPERATOR}-http.loadbalancer.server.port=443"
      #       - "traefik.http.services.${OPERATOR}-http.loadbalancer.server.scheme=https"
      #       - "traefik.http.routers.${OPERATOR}-https.tls=true"
      #       - "traefik.docker.network=traefik"
      #networks:
      #       - traefik
      #       - local_stack
      ports:
              - ${WEB_IHM_PORT}:80
              - ${WEB_IHM_SSL_PORT}:443
              - ${LOGHOST_PORT}:514/udp
      volumes:
          - 'hm-web_vol01:/var/www/hotspotmanager/prod/web/'
          - 'hm-logs_vol01:/data/logs/'
         #- 'ssl-certificates_vol01:/root/hm_ssl_certificates/'
          - '/root/ssl_certificates/:/root/hm_ssl_certificates/'
         #- '/backups/:/backups/'
      depends_on:
        - "hm-db"
        
 hm-radius-v3:
      image: docker.adipsys.com:5000/hm-ha-radius-v3:${PRODUCT_VERSION}
      restart: always
      environment:
             - DEBUG=0
      #command: "freeradius -XXX"
      #deploy:
         #placement:
             #max_replicas_per_node: 1
      #networks:
      #       - traefik
      #       - local_stack
      ports:
              - ${RADIUS_AUTH_PORT}:1812/udp
              - ${RADIUS_ACC_PORT}:1813/udp
              #- target: ${RADIUS_AUTH_PORT}
              #  published: 1812
              #  protocol: udp
              #  mode: host
              #- target: ${RADIUS_ACC_PORT}
              #  published: 1813
              #  protocol: udp
              #  mode: host
      depends_on:
        - "hm-db"
